#!/bin/bash

# TODO: Try to uninstall playwright too. Either delete the files directly or call playwright uninstall

rm ~/.local/bin/swipe
# TODO: uninstall.sh is located here. Do some magic to allow deleting itself. Copy to tmp and execute from there?
# rm -rf ~/.local/share/wowup

# TODO: Make these optional, like ./uninstall.sh --full
# rm -rf ~/.cache/wowup
# rm -rf ~/.config/wowup
